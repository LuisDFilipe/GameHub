# Games Hub

A Vue 3 + TypeScript PWA with a required player profile screen, a game list, Tic-Tac-Toe against the CPU, a memory game, and local score tracking.

## Local Setup

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
